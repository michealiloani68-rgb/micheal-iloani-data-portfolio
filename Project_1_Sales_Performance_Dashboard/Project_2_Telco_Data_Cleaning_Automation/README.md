# 🟩 Project 2 – Telco Data Cleaning & Automation (Python + Pandas)

## 1. Project Overview

This project uses a Telco Customer Churn dataset to demonstrate a repeatable data cleaning and preparation pipeline.

The focus is on:

- Handling missing and inconsistent values
- Standardising fields
- Producing a clean dataset ready for analysis or modelling
- Generating a simple data-quality report

---

## 2. What this project shows

- Python (Pandas) for data cleaning
- Basic automation (script you can rerun any time)
- Data validation and quality checks
- Creating “raw” vs “cleaned” datasets

---

## 3. Files in this folder

- `data/telco_customer_churn.csv` – raw dataset
- `scripts/telco_cleaning_pipeline.py` – Python cleaning script
- `outputs/telco_cleaned.csv` – cleaned data
- `outputs/data_quality_report.md` – summary of data quality

---

## 4. How to run

1. Place the raw dataset in `data/telco_customer_churn.csv`.
2. Install Python 3 (if not already installed).
3. Install dependencies:
   ```bash
   pip install pandas numpy
4. Run the script:
   python scripts/telco_cleaning_pipeline.py
5. Check outputs in the outputs/ folder.
